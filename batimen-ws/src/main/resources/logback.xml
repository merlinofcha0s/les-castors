<configuration scan="true" debug="true">
	<contextName>batimenContext</contextName>

	<property name="LOGS_FOLDER" value="c:/logs/web" />
	<property name="LOGS_TO_COLLECT_FOLDER" value="c:/logs/web/archived" />

	<!-- Fait pour la prod, ne log qu'a partir du lvl warn et archive les fichiers 
		par date -->
	<appender name="log-file"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_FOLDER}/batimen.log</file>
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>WARN</level>
		</filter>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOGS_TO_COLLECT_FOLDER}/batimen.%d{yyyyMMdd}.log
			</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>%d{yyyy/MM/dd HH:mm:ss,SSS} %-5level %logger{36} - %m
				%throwable{0}%n</pattern>
		</encoder>
	</appender>

	<!-- Level de log dependant du root. Efface le fichier des qu'il atteint 
		10Mo -->
	<appender name="troubleshooting-file"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_FOLDER}/batimen-troubleshooting.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>${LOGS_FOLDER}/batimen-troubleshooting.%i.log
			</fileNamePattern>
			<maxIndex>10</maxIndex>
		</rollingPolicy>
		<triggeringPolicy
			class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>10MB</maxFileSize>
		</triggeringPolicy>
		<encoder>
			<pattern>%d{yyyy/MM/dd HH:mm:ss,SSS} %-5level %logger{36} - %m%n
			</pattern>
		</encoder>
	</appender>


	<!-- Permet d'avoir les logs dans la console d'eclipse et glassfish (a degager 
		en prod) -->
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder 
			by default -->
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>


	<root level="DEBUG">
		<appender-ref ref="stdout" />
	</root>
</configuration>