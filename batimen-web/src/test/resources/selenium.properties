app.ip=${app.ip}
app.port=${app.port}
app.name=${app.name}
app.browser.target=${app.browser.target}
app.address.chrome.driver=${app.address.chrome.driver}
app.address.ie.driver=${app.address.ie.driver}
#Chemin où l'on stocke les images qui seront utilisé pour tester l'upload
app.temp.img.dir.test=${app.temp.img.dir.test}