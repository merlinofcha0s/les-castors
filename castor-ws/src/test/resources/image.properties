temp.img.dir=${prop.temp.img.dir}
cloud.name=${prop.image.cloudinary.cloud_name}
api.key=${prop.image.cloudinary.api_key}
api.secret=${prop.image.cloudinary.api_secret}
is.test=${prop.image.is.test}
#Chemin où l'on stocke les images qui seront utilisé pour tester l'upload
app.temp.img.dir.test=${app.temp.img.dir.test}